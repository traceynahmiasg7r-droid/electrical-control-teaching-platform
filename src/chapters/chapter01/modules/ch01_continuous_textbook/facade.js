(function installCh01ContinuousTextbookFacade(global) {
  "use strict";
  const platform = global.ECTPPlatform = global.ECTPPlatform || {};
  platform.moduleFacades = platform.moduleFacades || {};
  const MODULE_ID = "ch01_continuous", ROUTE_ID = "ch01-continuous-control";
  const clone = (value) => value === undefined ? undefined : JSON.parse(JSON.stringify(value));
  platform.moduleFacades.createCh01ContinuousTextbookFacade = ({ context, circuitData }) => {
    const contracts = platform.contracts, solver = platform.moduleSolvers.ch01ContinuousTextbook, teaching = platform.ch01ContinuousTextbookTeaching;
    const renderer = platform.moduleRenderers.createCh01ContinuousTextbookRenderer(circuitData);
    let state = solver.createInitialState(), result, mounted = false, paused = false, inputController = null;
    const pointerOwners = new Map(), keyOwners = new Map(), canvasOwners = new Map();
    const playback = platform.createReversePlayback({ scope: context.scope, teaching,
      evaluate(spec) { let solved = solver.solve(solver.createInitialState()); for (const item of spec.actions) solved = solver.solve(solver.reduce(solved.state, item.command, item.payload || {})); return clone(solved); },
      onChange() { refresh(); } });
    function solveNow() { const solved = solver.solve(state); state = solved.state; result = solved.solverResult; return result; }
    const solvedRaw = (raw) => raw?.state && raw?.solverResult ? raw : { state, solverResult: result };
    const displayed = () => solvedRaw(playback.current()?.displayState?.raw);
    const ownersFor = (command) => [...pointerOwners.values(), ...keyOwners.values()].filter((owner) => owner === command).length + (canvasOwners.get(command) || 0);
    const owners = () => pointerOwners.size + keyOwners.size + [...canvasOwners.values()].reduce((total, count) => total + count, 0);
    function snapshot(raw) { const solved = solvedRaw(raw), op = solved.state.operationState, out = solved.solverResult, motor = out.motorStates.M; return { schemaVersion: contracts.facadeSchemaVersion, moduleId: MODULE_ID, routeId: ROUTE_ID, operation: { power: op.power, scene: "continuous", controls: clone(op.buttons), protections: {} }, devices: { KM: { id: "KM", energized: Boolean(out.stableDeviceStates.KM) }, SB1: { id: "SB1", pressed: op.buttons.sb1 === "pressed" }, SB2: { id: "SB2", pressed: op.buttons.sb2 === "pressed" } }, motor: { id: "M", state: motor.running ? "running" : "stopped", running: motor.running, direction: motor.direction, extension: { M: clone(motor) } } }; }
    function normalized(raw) { const out = solvedRaw(raw).solverResult; return { ...contracts.createEmptySolverResult(MODULE_ID), ...clone(out), schemaVersion: contracts.facadeSchemaVersion, moduleId: MODULE_ID, activeWireIds: [...new Set([...(out.activeMainWireIds || []), ...(out.activeControlWireIds || [])])] }; }
    function visual(raw) { const solved = solvedRaw(raw), out = solved.solverResult; return { ...normalized(raw), pressed: clone(solved.state.operationState.buttons), stableDeviceStates: clone(out.stableDeviceStates), motorStates: clone(out.motorStates), edgeStates: clone(out.edgeStates) }; }
    function operationView() { const op = displayed().state.operationState; const controls = [
      { slot: "primary", visible: true, label: "按住启动 · SB1", stateText: op.buttons.sb1 === "pressed" ? "按住中 · 松开后自锁" : "按住启动 · 松开后保持", buttonClass: "forward", action: "JOG_PRESS", payload: { command: "start" }, momentary: true },
      { slot: "secondary", visible: true, label: "按住停止 · SB2", stateText: op.buttons.sb2 === "pressed" ? "按住中 · NC 断开" : "按住停止 · NC 闭合", buttonClass: "stop", action: "JOG_PRESS", payload: { command: "stop" }, momentary: true }
    ]; return { schemaVersion: contracts.facadeSchemaVersion, moduleId: MODULE_ID, power: { deviceId: "SUPPLY", layout: "single-toggle", closed: op.power === "closed", closeLabel: "接通电源", openLabel: "断开电源", closeEnabled: op.power !== "closed", openEnabled: op.power === "closed" }, controls, protections: [], protection: { visible: false }, actionStates: controls.map((item) => ({ id: item.payload.command, label: item.label, currentState: item.stateText, availableTransitions: ["JOG_PRESS", "JOG_RELEASE"], onAction: item.action, feedbackText: "SB1 启动并自锁；SB2 常闭停止优先。" })), extension: { powerMeaning: "外部供电环境，教材中未画电源开关", hasSelfHold: true } }; }
    function statusView() { const solved = displayed(), op = solved.state.operationState, out = solved.solverResult, running = out.motorStates.M.running, row = (id, label, value, active) => ({ id, label, value, tone: active ? "on" : "off" }); return { schemaVersion: contracts.facadeSchemaVersion, moduleId: MODULE_ID, rows: [row("supply", "外部电源", op.power === "closed" ? "已接通" : "已断开", op.power === "closed"), row("sb1", "启动按钮 SB1", op.buttons.sb1 === "pressed" ? "按下 · NO 闭合" : "释放 · NO 断开", op.buttons.sb1 === "pressed"), row("sb2", "停止按钮 SB2", op.buttons.sb2 === "pressed" ? "按下 · NC 断开" : "释放 · NC 闭合", op.buttons.sb2 !== "pressed"), row("km", "KM 线圈", out.stableDeviceStates.KM ? "得电吸合" : "失电释放", out.stableDeviceStates.KM), row("self", "KM 自锁触点", out.stableDeviceStates.KM ? "闭合保持" : "断开", out.stableDeviceStates.KM), row("motor", "电动机 M", running ? "长动运行" : "已停止", running), row("hold", "保持方式", "SB1 松开后由 KM 自锁；SB2 停止优先", out.stableDeviceStates.KM)] }; }
    function feedback() { const current = playback.current(); return current ? { title: current.title, text: current.text, tone: "info", source: "Playback" } : { ...teaching.liveFeedback(snapshot(), result), source: "Solver" }; }
    function playbackView() { const current = playback.current(), raw = current?.displayState?.raw; return { ...clone(playback.view()), displayState: { source: current ? "Playback" : "Live", visualState: visual(raw), solverResult: normalized(raw), snapshot: snapshot(raw), teachingFocus: current?.displayState?.teachingFocus || [] } }; }
    function refresh() { if (mounted && !paused) { render(); context.services?.renderShell?.(); } }
    function clearOwners() { pointerOwners.clear(); keyOwners.clear(); canvasOwners.clear(); }
    function releaseAll() { clearOwners(); state = solver.reduce(state, "release"); solveNow(); }
    function releaseOwned(command, source) { if (ownersFor(command)) return; dispatch(contracts.createAction("JOG_RELEASE", { command }, source)); }
    function render() { if (!mounted || paused || !context.mountRoot) return; const view = playbackView().displayState; renderer.render({ root: context.mountRoot, visualState: view.visualState, source: view.source, teachingFocus: view.teachingFocus, onAction(id, value) { const command = typeof value === "string" ? value : value?.command; if (id === "pressControl") { canvasOwners.set(command, (canvasOwners.get(command) || 0) + 1); dispatch(contracts.createAction("JOG_PRESS", { command }, "module-canvas")); } else if (id === "releaseControl") { const remaining = Math.max(0, (canvasOwners.get(command) || 0) - 1); if (remaining) canvasOwners.set(command, remaining); else canvasOwners.delete(command); releaseOwned(command, "module-canvas"); } } }); context.services?.onModuleRender?.(MODULE_ID); }
    function dispatch(input) { const action = typeof input === "string" ? contracts.createAction(input) : input, report = contracts.validateAction(action); if (!report.valid) throw new Error(report.errors.join("; ")); const command = action.payload.command || ({ POWER_CLOSE: "powerClose", POWER_OPEN: "powerOpen", POWER_TOGGLE: "powerToggle", JOG_PRESS: "start", JOG_RELEASE: "release", START_PRIMARY_PRESS: "start", STOP_PRIMARY_PRESS: "stop", START_SECONDARY_PRESS: "stop", RESET_MODULE: "reset" })[action.type]; const actual = action.type === "JOG_RELEASE" ? "release" : command; if (!["powerClose", "powerOpen", "powerToggle", "start", "stop", "release", "reset"].includes(actual)) throw new Error(`Unsupported textbook continuous action: ${action.type}`); playback.cancel(); if (actual === "reset") { clearOwners(); renderer.resetInput?.(); } state = solver.reduce(state, actual, action.payload); solveNow(); refresh(); const output = { action, state: snapshot(), solverResult: normalized(), operationViewModel: operationView(), statusViewModel: statusView(), feedback: feedback() }; contracts.assertFacadeOutputs({ meta: { moduleId: MODULE_ID }, getStateSnapshot: () => output.state, normalizeSolverResult: () => output.solverResult, getOperationViewModel: () => output.operationViewModel, getStatusViewModel: () => output.statusViewModel }); return output; }
    function attachInputs() { inputController?.abort(); inputController = new AbortController(); const doc = global.document; if (!doc) return; const options = { capture: true, signal: inputController.signal }; const controlAt = (target) => { const el = target?.closest?.("#pressSb1:not(.hidden):not(:disabled), #pressSb2:not(.hidden):not(:disabled)"); if (!el) return null; return el.id === "pressSb1" ? "start" : "stop"; }; const swallow = (e) => { e.preventDefault(); e.stopImmediatePropagation(); }; const press = (command, source) => dispatch(contracts.createAction("JOG_PRESS", { command }, source)); doc.addEventListener("pointerdown", e => { const c = controlAt(e.target); if (!mounted || paused || e.button !== 0 || !c) return; swallow(e); pointerOwners.set(e.pointerId, c); press(c, "module-shell-pointer"); }, options); const end = e => { const c = pointerOwners.get(e.pointerId); if (!c) return; swallow(e); pointerOwners.delete(e.pointerId); releaseOwned(c, "module-shell-pointer"); }; doc.addEventListener("pointerup", end, options); doc.addEventListener("pointercancel", end, options); doc.addEventListener("keydown", e => { const c = controlAt(e.target); if (!mounted || paused || !c || ![" ", "Enter"].includes(e.key) || e.repeat) return; swallow(e); keyOwners.set(e.key, c); press(c, "module-shell-keyboard"); }, options); doc.addEventListener("keyup", e => { const c = keyOwners.get(e.key); if (!c) return; swallow(e); keyOwners.delete(e.key); releaseOwned(c, "module-shell-keyboard"); }, options); doc.addEventListener("click", e => { const c = controlAt(e.target); if (!mounted || paused || !c) return; swallow(e); if (e.detail === 0 && !owners()) { press(c, "module-shell-tap"); releaseOwned(c, "module-shell-tap"); } }, options); global.addEventListener?.("blur", () => { if (owners()) { releaseAll(); refresh(); } }, { signal: inputController.signal }); }
    context.scope?.addCleanup(() => { inputController?.abort(); clearOwners(); }); solveNow();
    return Object.freeze({ createInitialState(seed = {}) { playback.cancel(); clearOwners(); renderer.resetInput?.(); state = solver.createInitialState(seed); solveNow(); return snapshot(); }, getStateSnapshot: snapshot, dispatchAction: dispatch, normalizeSolverResult: normalized, getOperationViewModel: operationView, getStatusViewModel: statusView, buildTeachingFeedback: feedback, getPlaybackViewModel: playbackView, buildReplaySteps: () => clone(playback.steps()), dispatchPlayback(command, value) { return playback.command(command, value); }, solve() { playback.cancel(); solveNow(); return normalized(); }, mount() { mounted = true; paused = false; global.document?.body?.classList.add("ch01-continuous-textbook-active"); attachInputs(); }, render, reset() { playback.cancel(); clearOwners(); renderer.resetInput?.(); state = solver.createInitialState(); solveNow(); refresh(); return snapshot(); }, pause() { paused = true; playback.cancel(); releaseAll(); renderer.resetInput?.(); }, resume() { paused = false; refresh(); }, unmount() { mounted = false; paused = false; playback.cancel(); inputController?.abort(); inputController = null; releaseAll(); renderer.unmount({ root: context.mountRoot }); global.document?.body?.classList.remove("ch01-continuous-textbook-active"); }, validateGeometry: () => circuitData.validateGeometry(), runTests: () => solver.runTests() });
  };
})(globalThis);
